// Importing the Externe Modules
import contactForm from "./contactForm.js";
import navbarButton from "./navbarButton.js";

// On documeme load execute this functions
document.addEventListener("DOMContentLoaded", e =>{
    navbarButton();
    contactForm();
});